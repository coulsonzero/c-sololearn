#include <stdio.h>

/**
 * arithmetic operators : + - *  / %
 * assignment operators : += -= *= /= %=
 * increment & Decrement: i++ i-- ++i --i
 * relational operators : == != < > <= >=
 * logical   operators  : && || !
 * a ? b : c
 * | ^ ~ << >>
 */


int main() {
    int x, y;
    x = ++y;

    printf("%d", x);    // 1
    return 0;
}


